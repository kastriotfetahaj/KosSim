{{- range $name, $id := .Values.grafanaCommDashboards }}
---
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: {{ $name }}
spec:
  instanceSelector:
    matchLabels:
      dashboards: ctf-grafana
  folder: ""
  grafanaCom:
    id: {{ $id }}
{{end}}
