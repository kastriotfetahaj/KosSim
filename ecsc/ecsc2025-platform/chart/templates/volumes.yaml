---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: platform-django
  labels:
    {{ include "platform.labels" . | nindent 4}}
spec:
  storageClassName: longhorn-core-replicated
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi
  volumeMode: Filesystem
